<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.omnist</groupId>
    <artifactId>omnist-j</artifactId>
    <version>0.2.1-alpha</version>
    <name>omnist-j</name>
    <description>Java implementation of the Omnist data-interchange specification</description>
    <url>https://j.omnist.dev</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>tomlee</id>
            <name>Thomas Lee</name>
            <url>https://github.com/tomlee</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/omnist-dev/omnist-j.git</connection>
        <developerConnection>scm:git:git@github.com:omnist-dev/omnist-j.git</developerConnection>
        <url>https://github.com/omnist-dev/omnist-j</url>
        <tag>HEAD</tag>
    </scm>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <junit.version>5.11.0</junit.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.18.9</version>
        </dependency>
        <dependency>
            <groupId>org.tomlj</groupId>
            <artifactId>tomlj</artifactId>
            <version>1.1.1</version>
        </dependency>
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>2.2</version>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>net.jqwik</groupId>
            <artifactId>jqwik</artifactId>
            <version>1.9.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>1.37</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <version>1.37</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <!--
                                dev.omnist.conformance is excluded from coverage metrics.
                                Its primary verification is running ./run-conformance against
                                omnist-spec's 181 live test-suite vectors, not exhaustive unit
                                coverage — identical justification to omnist-go CI's exclusion
                                of tools/conformance/ from its own 100% bar.

                                CliMain is excluded because it is a one-line process entry point
                                (System.exit after delegating to Cli.run, which itself carries
                                all real logic and is fully covered/tested).
                            -->
                            <excludes>
                                <exclude>dev/omnist/conformance/**</exclude>
                                <exclude>dev/omnist/cli/CliMain.class</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <!--
                                dev.omnist.conformance excluded: verified by live harness
                                (181/0/0), not unit tests — see omnist-go tools/conformance/.
                                CliMain excluded: one-line System.exit entry point, all real
                                logic lives in the fully-covered Cli.run.
                            -->
                            <excludes>
                                <exclude>dev/omnist/conformance/**</exclude>
                                <exclude>dev/omnist/cli/CliMain.class</exclude>
                            </excludes>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <!-- 99.82% measured 2026-08-16 (excl. dev.omnist.conformance), after
                                                 #34's full LINE+BRANCH investigation: the only 5 remaining
                                                 gate-scoped missed lines are all confirmed-unreachable defensive
                                                 code (regex/DOM/Jackson contract guarantees; see comments at
                                                 OmlLexer's two NumberFormatException catches and JsonCodec.write's
                                                 IOException catch). JaCoCo has no line-level exclusion mechanism
                                                 (only class-level, already used for
                                                 dev.omnist.conformance/CliMain above), so these stay inline rather
                                                 than being fragmented into dedicated excluded classes purely to
                                                 chase the metric. Gate set just below the real achieved number so
                                                 it catches actual regressions immediately rather than sitting on a
                                                 stale floor. -->
                                            <minimum>0.998</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <!-- 99.85% measured 2026-08-16 (excl. dev.omnist.conformance). #34's
                                                 branch-coverage pass closed 36 of the 39 gate-scoped gaps found at
                                                 the start of that investigation: 2 real JaCoCo false negatives
                                                 fixed (bare-continue GOTOs), 2 real dead branches deleted (proven
                                                 unreachable by static analysis, not just unobserved), and the rest
                                                 closed with real-input test fixtures or reflection-based tripwire
                                                 tests for genuinely-unreachable defensive code. The 3 remaining
                                                 gaps are the branch counterparts of the LINE gate's own
                                                 confirmed-unreachable lines (OmlLexer/Materializer's twin
                                                 parseTimeValue sign-offset checks, XmlCodec's Text.getNodeValue()
                                                 null check), same documented-in-place convention as LINE.

                                                 Gate set with a larger margin than LINE's (0.995 vs. the 99.85%
                                                 real number, not 0.998), because BRANCH, unlike LINE, is
                                                 measurably sensitive to jqwik's RANDOMIZED fuzz-test seeding:
                                                 FuzzTest/OmlLexerPropertyTest/TomlRadixPropertyTest pick a fresh
                                                 seed every run, so which combinatorial branch outcomes a given
                                                 run's random inputs happen to hit can legitimately shift by a
                                                 branch or two run to run. A CI run at exactly 0.998 failed on
                                                 real, non-regressive variance (0.997 measured) shortly after this
                                                 gate was first set at 0.998; this margin is the fix. -->
                                            <minimum>0.995</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.3.1</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.10.0</version>
                <configuration>
                    <doctitle>omnist-j ${project.version} API</doctitle>
                    <windowtitle>omnist-j ${project.version} API</windowtitle>
                    <quiet>true</quiet>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <!--
                                Attached as a separate "cli" classifier rather than replacing the
                                main artifact: the main dev.omnist:omnist-j jar must stay a plain
                                library jar with its real <dependencies> intact for anyone adding
                                it as a Maven/Gradle dependency, so Jackson/SnakeYAML/tomlj don't
                                get silently duplicated on their classpath. The fat jar is only for
                                running `omnist` as a standalone CLI (see the wrapper script and
                                Track1Runner.java's conformance harness, both of which reference
                                the -cli.jar explicitly).
                            -->
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>cli</shadedClassifierName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>dev.omnist.cli.CliMain</mainClass>
                                </transformer>
                            </transformers>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <phase>package</phase>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Signing and Central publishing are opt-in via `mvn -Prelease ...` rather than
            bound to the default lifecycle, so ordinary `mvn clean test`/`mvn package` (what
            CI and every contributor runs) never requires a GPG key to be present. Activate
            with `mvn clean deploy -Prelease` once ~/.m2/settings.xml has the `central` server
            credentials (see docs/workflow-playbook.md) and a GPG key is available locally.
        -->
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                        <version>3.10.0</version>
                        <executions>
                            <execution>
                                <id>attach-javadocs</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jar</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.7</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.11.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <!--
                                Manual review before publish: the bundle uploads to the Portal
                                but requires an explicit click there to actually release. Flip to
                                true only once confident enough in the release process to skip
                                that manual check; Central releases are immutable, there is no
                                undo after publish.
                            -->
                            <autoPublish>false</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
